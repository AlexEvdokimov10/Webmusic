import React, {memo} from 'react';
import UserData from "./UserData";

const AdminPanel =() => {
    return (
        <div>
            <UserData/>
        </div>
    );
};

export default AdminPanel;